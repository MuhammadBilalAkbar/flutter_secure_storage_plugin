name: flutter_secure_storage_plugin
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Flutter Secure Storage
  flutter_secure_storage: ^7.0.1
  # Firebase For Authentication
  firebase_core: ^2.4.1
  firebase_auth: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true